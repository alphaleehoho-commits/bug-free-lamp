---
description: 開工前必須先拆包分工（用戶基層工序）
alwaysApply: true
---

# 基層工序：先拆包，後開工

用戶（alpha）**唔需要自己分區**。任何改動／bug list／功能需求，agent **必須**先完成拆包分工，**先至**寫 code、開 parallel subagent、或者開 PR。

## 必做順序

1. **讀需求**（可以唔齊、可以有截圖）
2. **拆包分工**（回覆用戶）：
   - 按遊戲區／系統分（例如：寵物、牧場／繁殖、派遣、秘境、潮淵、掛機／練功、進階、經濟／材料、UI 殼）
   - 標明：可平行／必須先後／會撞同一檔（尤其 `ui.js`／`engine.js`）
   - 建議第一輪開邊幾包、用唔用 multitask
3. **等用戶確認**，或者需求已寫「按你推薦直接去／去~」先開工
4. **開工**時按拆好嘅包執行；大包用 focused subagent，同區 follow-up 唔好亂開新 agent

## 禁止

- 一收到長 list 就即刻改 code、唔出拆包
- 一個 mega agent／一個 PR 硬食晒無關系統（除非用戶明確要求一次過同已拆包說明）
- 開「常駐區 agent」空等——只開有工開嘅 focused 任務

## 拆包回覆最低內容

- 包名 + 涵蓋點
- 依賴／衝突
- 建議執行次序（或平行組合）
